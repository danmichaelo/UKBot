# Add trailing slash
#$HTTP["url"] !~ "^(.*)(\..{2,3}|/)$" {
#        url.redirect = (".*" => "$1/")
#}

$HTTP["url"] !~ ".*\.[a-z]{2,3}$" {
    fastcgi.server += ( "/ukbot" =>
        ((
            "socket" => "/tmp/ukbot-fcgi.sock",
            "bin-path" => "/data/project/ukbot/server.sh",
            "check-local" => "disable",
            "max-procs" => 1,
        ))
    )
}

